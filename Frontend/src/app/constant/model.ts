export interface UserL{
    cityId: String,
    createdAt: String,
    image: String,
    name: String,
    updatedAt: String,
  };

  export type seatsEntry = {
    seatNumber:Number,
    row: String,
    isBooked: Boolean,
    price:Number,

  }